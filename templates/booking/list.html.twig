<td>
    <a href="{{ path('app_booking_update', {'id': booking.id}) }}" class="btn btn-primary btn-sm">Update</a>
    <a href="{{ path('app_booking_delete', {'id': booking.id}) }}" class="btn btn-danger btn-sm">Delete</a>
</td>